\documentclass[main.tex]{subfiles}

\begin{document}

\section{Heater Box Overview}

\subsection{Purpose}

The Heater Control Box is an introductory process control trainer designed to teach PLC-based temperature regulation. It provides a self-contained thermal plant with all necessary actuators, sensors, and operator interface elements wired to the PLC through the DB37 passthrough board. Students use this box to learn digital and analog I/O configuration, PID loop tuning, and alarm handling in a safe, low-power environment.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/HeaterBoxConcept.png}
    \caption{Heater Control Box system diagram. The PLC drives indicator LEDs, a heater relay, a fan relay, and an analog voltmeter through the DB37 passthrough; it reads pushbuttons, a setpoint potentiometer, and a temperature signal from the Type-K thermocouple via the ProSense XTD2 4--20\,mA transmitter. The 150\,\si{\ohm} heater warms the thermocouple to close the control loop.}
    \label{fig:heaterbox-concept}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{"images/Current Design/Htr box Din.png"}
    \caption{Digital input sheet. The 5069-IB16 module reads up to 16 discrete 24\,VDC inputs (channels 0--9 used) through a DB37 adapter. Two pushbuttons are wired to the first input channels.}
    \label{fig:heaterbox-din}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{"images/Current Design/Htr box Dout.png"}
    \caption{Digital output sheet. The 5069-OB16 module provides 16 discrete outputs (0.5\,A per channel, 8\,A per module) through a DB37 adapter. Outputs drive the Green, Yellow, and Red indicator LEDs, a fan relay, and a heater relay switching current through the 150\,\si{\ohm} resistive heater element.}
    \label{fig:heaterbox-dout}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{"images/Current Design/Htr box Ain.png"}
    \caption{Analog input sheet. The 5069-IY4 module provides four differential analog input channels routed through a DB37 adapter. Channel\,0 reads the front-panel potentiometer (0--10\,V setpoint) and channel\,1 receives the 4--20\,mA signal from the ProSense thermocouple transmitter.}
    \label{fig:heaterbox-ain}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{"images/Current Design/Htr box Aout.png"}
    \caption{Analog output sheet. The 5069-OF4 module provides four analog output channels through a DB37 adapter. One channel drives the front-panel analog voltmeter for process variable or control output visualization.}
    \label{fig:heaterbox-aout}
\end{figure}

\subsection{Theory of Operation}

The control loop operates as follows:

\begin{enumerate}
    \item The PLC energizes a digital output to close the \textbf{heater relay}, which switches current through a \textbf{150\,\si{\ohm} resistive heater element}.
    \item The heater (resistor) wams and is monitored by a \textbf{Type K thermocouple}.
    \item The thermocouple signal is conditioned by a \textbf{ProSense XTD2} temperature transmitter, which converts it to a 4--20\,mA current loop signal proportional to temperature.
    \item The 4--20\,mA signal feeds an analog input channel on the PLC (5069-IY4), where it is scaled to engineering units.
    \item The PLC executes a control algorithm (on/off, P, PI, or PID) and modulates the heater relay output accordingly.
    \item A \textbf{fan}, controllable via a separate PLC digital output, can be activated to introduce a thermal disturbance or to accelerate cooling, providing students with a means to test disturbance rejection.
\end{enumerate}

\subsection{Operator Interface Elements}

The front panel provides the following elements for student interaction and process visualization:

\begin{table}[H]
\centering
\caption{Heater Box Front Panel Elements}
\label{tab:front-panel}
\begin{tabularx}{\textwidth}{l l X}
\hline
\textbf{Element} & \textbf{Quantity} & \textbf{Function} \\
\hline
Indicator LEDs & 5 (2$\times$ Green, Yellow, 2$\times$ Red) & Status indication driven by PLC digital outputs (e.g., heater on, alarm, run/stop) \\
Pushbuttons & 2 & Operator inputs to PLC digital inputs (e.g., start/stop, acknowledge) \\
Potentiometer & 1 & Analog setpoint input (voltage divider providing 0--10\,V to PLC analog input) \\
Fan & 1 & Thermal disturbance element, driven by PLC digital output \\
Analog Voltmeter & 1 & Visual display of a PLC analog output (e.g., process variable or control output) \\
\hline
\end{tabularx}
\end{table}

\end{document}
